
import 'package:booking/Customs/CustomBookingPage.dart';

import 'package:flutter/material.dart';

class scand extends StatelessWidget {
  const scand({super.key});

  @override
  Widget build(BuildContext context) {
    return CustomBookingPage(
      name: "",
      number: "",
    );
  }
}
